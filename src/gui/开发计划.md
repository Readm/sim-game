# 网络仿真查看器开发计划

## 已完成工作 ✅

### 1. 基础框架搭建
- [x] 创建CMake构建系统
- [x] 设置基本的GUI框架（ImGui + GLFW）
- [x] 实现NetworkViewer基本类结构
- [x] 实现HTTP客户端基本功能

### 2. 界面组件
- [x] 实现基本菜单栏
- [x] 实现模拟控制面板
- [x] 实现节点编辑器基本框架
- [x] 实现属性面板基本框架

### 3. 测试框架
- [x] 实现基本的GUI测试框架
- [x] 解决OpenGL和ImGui相关问题
- [x] 实现基础可视化测试功能
- [x] 实现模拟控制测试
- [x] 改进可视化测试模式，支持所有测试显示界面
- [x] 实现更完善的示例节点编辑器界面
- [x] 完善测试框架类成员变量和网络状态检测功能
- [x] 修复窗口显示问题（添加窗口尺寸变量和临时修复连接状态检测）

### 4. 前后端分离架构
- [x] 实现基本的HTTP客户端类 (NetworkClient)
- [x] 添加模拟服务器 (MockServer) 用于测试
- [x] 修改NetworkViewer支持远程服务器连接
- [x] 实现基本的RESTful API通信
- [x] 添加WebSocket状态更新模拟
- [x] 实现集成测试用例
- [x] 添加错误处理和网络延迟模拟

## 待完成工作 🚧

### 1. 测试框架完善（当前重点）
- [ ] 完善离屏渲染支持
- [ ] 改进事件注入系统
- [ ] 实现更精确的状态验证机制
- [ ] 添加测试覆盖率分析
- [ ] 添加测试截图和报告生成功能

### 2. 后端服务器开发
- [ ] 创建HTTP服务器基本框架
- [ ] 实现模拟控制API接口
- [ ] 实现网络状态序列化/反序列化
- [ ] 实现WebSocket实时状态推送
- [ ] 添加认证和安全措施

### 3. 节点编辑功能
- [ ] 实现节点创建功能
- [ ] 实现节点删除功能
- [ ] 实现端口连接功能
- [ ] 实现节点属性编辑
- [ ] 实现节点位置保存/加载

### 4. 模拟控制功能
- [ ] 实现模拟速度控制
- [ ] 实现断点设置功能
- [ ] 实现状态回放功能
- [ ] 实现日志查看功能

### 5. 文件操作功能
- [ ] 实现网络保存功能
- [ ] 实现网络加载功能
- [ ] 实现导出/导入功能

### 6. 性能优化
- [ ] 实现大规模网络的高效渲染
- [ ] 实现节点数据的延迟加载
- [ ] 实现状态更新的增量传输

## 前后端分离架构设计 🔄

### 总体架构

```
+----------------+      HTTP/WebSocket     +----------------+
|                |<----------------------->|                |
|  前端 (GUI)     |                        |  后端 (服务器)   |
|                |                        |                |
+----------------+                        +----------------+
| - NetworkViewer|                        | - 模拟引擎       |
| - ImGui界面     |                        | - 网络状态管理   |
| - HTTP客户端    |                        | - REST API     |
| - WebSocket客户端|                        | - WebSocket服务|
+----------------+                        +----------------+
```

### 接口设计

#### REST API
- `GET /api/status` - 获取服务器状态
- `GET /api/network/state` - 获取当前网络状态
- `POST /api/simulation/start` - 启动模拟
- `POST /api/simulation/stop` - 停止模拟
- `POST /api/simulation/step` - 单步模拟
- `POST /api/simulation/reset` - 重置模拟
- `PUT /api/network/nodes/:id` - 更新节点属性
- `POST /api/network/connections` - 创建连接
- `DELETE /api/network/connections/:id` - 删除连接

#### WebSocket 事件
- `state_update` - 网络状态实时更新
- `simulation_status` - 模拟状态更新
- `error` - 错误通知

### 已实现组件

1. **NetworkClient**: 负责HTTP通信和状态获取
2. **MockServer**: 模拟后端服务器
3. **前后端集成测试**: 验证通信功能
4. **WebSocket更新测试**: 验证实时更新功能

### 后续改进方向

1. 实现真实后端服务器
2. 完善网络安全和认证
3. 添加数据持久化
4. 实现大规模网络的高效同步
5. 添加错误恢复和断线重连机制

## 单元测试设计 🧪

### 1. GUI测试框架设计
```cpp
// 测试框架结构
class GuiTestFramework {
public:
    // 模拟用户输入
    void simulateMouseClick(int x, int y);
    void simulateKeyPress(int key);
    void simulateDragAndDrop(int startX, int startY, int endX, int endY);
    
    // 状态验证
    bool verifyWindowExists(const std::string& title);
    bool verifyNodeExists(int nodeId);
    bool verifyConnectionExists(int startPin, int endPin);
    
    // 模拟渲染循环
    void runOneFrame();
    void waitForSeconds(float seconds);
    
    // 新增: 显示控制
    void showVisualForSeconds(float seconds, std::function<void(int)> updateFunc);
};
```

### 2. 测试用例分类

#### 基础功能测试
- [x] 窗口创建和销毁测试
- [ ] 菜单操作测试
- [ ] 基本UI响应测试

#### 节点编辑测试
- [ ] 节点创建测试
- [ ] 节点删除测试
- [ ] 节点连接测试
- [ ] 节点属性修改测试

#### 模拟控制测试
- [x] 启动/停止测试
- [x] 单步执行测试
- [ ] 状态更新测试

#### 网络操作测试
- [ ] 网络保存测试
- [ ] 网络加载测试
- [ ] 状态序列化测试

### 3. 测试实现方案

1. ~~使用Google Test框架作为基础测试框架~~ 已改用doctest框架
2. [x] 创建模拟的OpenGL上下文
3. [ ] 实现离屏渲染支持
4. [x] 使用模拟的时间控制
5. [x] 实现事件注入系统
6. [x] 支持可视化测试模式切换 (--visual/-v 参数)

### 4. 示例测试代码
```cpp
TEST_CASE_FIXTURE(NetworkViewerFixture, "模拟控制测试") {
    MESSAGE("模拟控制测试");
    
    // 初始状态检查
    CHECK_FALSE(viewer.isSimulationRunning());
    CHECK_EQ(viewer.getCurrentTick(), 0);

    // 启动模拟
    showVisualForSeconds(1, [&](int) {
        MESSAGE("启动模拟");
        viewer.startSimulation();
    });
    CHECK(viewer.isSimulationRunning());

    // 停止模拟
    showVisualForSeconds(1, [&](int) {
        MESSAGE("停止模拟");
        viewer.stopSimulation();
    });
    CHECK_FALSE(viewer.isSimulationRunning());
}
```

## 下一步工作计划 📋

1. **完善测试框架**
   - 优化窗口显示持续时间控制
   - 实现更精确的状态验证函数
   - 添加截图功能用于测试报告
   - 实现测试结果可视化展示

2. **开始节点编辑功能开发**
   - 实现基本节点创建删除功能
   - 开发节点拖拽交互
   - 实现端口连接绘制

3. **后端集成**
   - 开始HTTP服务器框架搭建
   - 设计数据交换格式
   - 实现实时状态更新机制

## 开发注意事项 ⚠️

1. 保持代码模块化，便于测试
2. 使用依赖注入模式，方便mock对象
3. 分离UI逻辑和业务逻辑
4. 使用事件系统进行模块间通信
5. 保持良好的错误处理和日志记录
6. 测试代码应当单独管理，不影响主要功能代码 